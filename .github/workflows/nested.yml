name: deploy_nested_workflow_02
on:
  workflow_run:
    workflows: ["Build"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Deploy Application
      run: |
        # Your deployment commands here
        echo "Deploying the application..."
