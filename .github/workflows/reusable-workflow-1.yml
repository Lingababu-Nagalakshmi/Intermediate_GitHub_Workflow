name: Reusable Workflow 1
on: 
 workflow_call:
   inputs:
     node:
       required: true
       type: string


jobs:

  build:

   runs-on: ubuntu-latest

   steps:

     -uses: actions/checkout@v3

     -name: Output of input value

     run : |
      
       echo " The node version is to use is ": ${{ inputs.node }}
